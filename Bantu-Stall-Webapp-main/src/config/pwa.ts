export const PWA_CONFIG = {
  enablePush: true,
  enableA2HS: true,
  // If you already have a VAPID public key, paste it here (Base64 URL, 65-byte uncompressed key)
  // If empty, use the VAPID Key Generator tool at /tools/vapid to create one
  vapidPublicKey: '' as string,
};
